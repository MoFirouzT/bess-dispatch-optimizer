# Spec R2.2. Scenario generation + reduction

**Status:** Implemented (gate green; five resolved decisions below, 2026-07-08)
**Release:** R2  **Depends on:** R2.1 (the interval forecaster this samples from), R1.4a (walk-forward + leakage discipline), R1.4b (ENTSO-E loader for the integration path)

## Objective

A scenario layer in `bess.scenarios` that turns R2.1's calibrated price intervals into a *discrete, probability-weighted set of full-day price paths*, then reduces that large set (200 to 500 paths) to a small representative one (~50) that preserves the distribution within a stated probability-metric tolerance. This is the uncertainty *representation* the R2.3 stochastic/recourse layer optimizes over; R2.2 produces the scenario tree, it does not solve a dispatch over it.

## Formulation reference

**No optimizer delta.** R2.2 adds no constraint, variable, or objective term to the dispatch MILP; the R1 model is untouched and the scenario set is an *input* the (future) R2.3 program consumes. The one genuinely new piece of theory R2.2 introduces is the **probability-metric-based scenario reduction** (the Kantorovich/Wasserstein distance between discrete distributions, forward selection, and the redistribution rule). Per `CLAUDE.md` §1's one-section-per-part pattern it earns a brief, self-contained summary, now in [`formulation.md` §R2.2](../formulation.md#r22-scenario-generation--reduction-uncertainty-representation-no-optimizer-change): the scenario-set definition, the reduction distance and greedy algorithm, the stability rationale (why preserving that distance is the right proxy for preserving the eventual stochastic decision), and an explicit out-of-scope list, pinned to the governing reference.

## Governing reference

- **Governing (the new theory; scenario generation + stability):** J. Dupačová, N. Gröwe-Kuska & W. Römisch, *Scenario reduction in stochastic programming: an approach using probability metrics* (Math. Programming, 2003), together with H. Heitsch & W. Römisch, *Scenario reduction algorithms in stochastic programming* (Comput. Optim. Appl., 2003), for the fast-forward-selection and backward-reduction algorithms and the Kantorovich-distance stability bound. Textbook context from **A. King & S. Wallace, *Modeling with Stochastic Programming*** (Springer). See open question 1: which of these is named *governing* versus subordinate, given the textbook-first policy.
- **Secondary (stochastic-programming foundations, pointer only):** Birge & Louveaux, *Introduction to Stochastic Programming*; Shapiro, Dentcheva & Ruszczyński, *Lectures on Stochastic Programming* (free). Scope authority moves here for R2.3, not R2.2.
- **Notation reconciliation:** house style wins. A scenario is a price path $\pi^{(s)} = (\pi^{(s)}_1,\dots,\pi^{(s)}_T)$ on the existing UTC hourly schema (`π_t`, €/MWh, grid-side; conventions §1/§4), carrying probability $p_s$ with $\sum_s p_s = 1$. The reference's generic random vector maps onto this price path; the probability metric is written $D(\cdot,\cdot)$.
- **Alternatives considered:** *moment matching* (match target mean/variance/skew/correlation by construction) is a legitimate generator but couples scenario quality to a hand-chosen moment list; kept as noted-not-built. *Optimal-scenario-tree / nested multistage* construction is out of scope (R2.2 is a single-stage fan of day-ahead paths; multistage tree structure belongs with R2.3 recourse if ever). *Property-matching heuristics without a stability argument* are rejected in favour of the distance-based method with a published bound.

## Design decisions

- **[ADR-0017](../decisions/0017-residual-path-bootstrap-generation.md). Residual-path bootstrap for generation, not independent per-hour sampling.** R2.1 delivers *marginal* hour-by-hour intervals (CQR calibrates each target timestamp). Sampling each hour independently from its marginal would destroy the strong intra-day temporal correlation of prices (a real day is a smooth peak/trough shape, not 24 independent draws) and hand R2.3 unrealistically jagged paths. Instead, generate paths by adding **whole-day forecast-error vectors**, resampled (bootstrap) from the forecaster's historical residuals, onto the point forecast; this preserves the empirical temporal correlation of errors and reuses data R2.1 already holds.
- **[ADR-0018](../decisions/0018-forward-selection-over-kmeans.md). Fast forward selection (Heitsch-Römisch) as the primary reducer, k-means as the compared baseline.** Forward selection carries the published Kantorovich-distance guarantee and the redistribution rule that keeps the reduced measure a valid probability distribution; k-means on the paths is the pragmatic clustering alternative the master plan names, kept as the baseline the gate compares against (mirrors R2.1's CQR-vs-split pattern).

## Scope decisions

- **Generation source v1: R2.1 forecaster residuals.** Self-contained and token-free-testable: the generator consumes an `IntervalForecast` plus the forecaster's residual history, no new data feed. The master plan's ARIMA/GARCH-bootstrap alternative is a *second* generator path (parametric, on raw ENTSO-E) and is out of scope here unless open question 2 flips.
- **Single-stage fan, not a multistage tree.** R2.2 emits one stage of `S` day-ahead paths with probabilities; the here-and-now/recourse split is R2.3's to impose.

## Parameters / configuration

| Item | Where | Default |
| --- | --- | --- |
| `n_generate` (raw scenario count) | scenario config | `300` (master-plan band 200 to 500) |
| `n_reduced` (kept representatives) | scenario config | `50` |
| `method` (reducer) | scenario config | `"forward"` (`"kmeans"` baseline) |
| `metric` / path distance | scenario config | Euclidean on the €/MWh path (`p = 2` Kantorovich cost) |
| `generator` | scenario config | `"residual_bootstrap"` |
| random seed | scenario config | fixed (determinism gate) |

## Interfaces

```python
# src/bess/scenarios/  (imports bess.forecaster for IntervalForecast; feeds bess.stochastic; layering below)

@dataclass(frozen=True)
class ScenarioSet:
    paths: np.ndarray          # shape (S, T): S price paths over the T-hour horizon, €/MWh grid-side
    probs: np.ndarray          # shape (S,): probabilities, >= 0, sum to 1
    index: pd.DatetimeIndex    # the T target UTC timestamps the paths are indexed on

def generate_scenarios(
    forecast: "IntervalForecast",
    residuals: np.ndarray,     # historical day-error vectors, shape (M, T), from the forecaster calibration set
    *, n: int, seed: int,
) -> ScenarioSet:
    """Residual-path bootstrap: point forecast + resampled whole-day error vectors,
    equiprobable (p_s = 1/n). Preserves intra-day error correlation."""

def reduce_scenarios(
    scenarios: ScenarioSet, *, n_reduced: int, method: str = "forward",
) -> tuple[ScenarioSet, float]:
    """Return the reduced set and its Kantorovich distance to the original.
    'forward' = Heitsch-Romisch fast forward selection with probability
    redistribution to the nearest kept path; 'kmeans' = cluster, centroids as
    representatives, cluster mass as probability."""

def kantorovich_distance(a: ScenarioSet, b: ScenarioSet, *, p: int = 2) -> float:
    """Discrete Wasserstein-p (Kantorovich) distance between two path measures."""
```

## Layering (import-linter)

`bess.scenarios` fills the already-declared "Forecast feed" contract slot `stochastic <- scenarios <- forecaster`. It **may import `bess.forecaster`** (for `IntervalForecast`) and `bess.data`; it must **not** import `stochastic`/`recourse`/`optimizer`/the serving chain. Enforced by import-linter: 4 contracts KEPT.

## Build tasks

- [x] `bess.scenarios.generate.generate_scenarios`: residual-path bootstrap onto the point forecast; equiprobable `ScenarioSet`; fixed-seed deterministic.
- [x] `bess.scenarios.reduce.reduce_scenarios`: fast forward selection with redistribution (primary) + k-means (baseline) behind the `method` switch; returns the reduced set and its distance.
- [x] `bess.scenarios.metrics.kantorovich_distance`: discrete Wasserstein-p between two path measures (the gate's yardstick).
- [x] Golden + property gates (below), token-free on synthetic/hand-built sets.
- [x] A count-vs-distance / count-vs-solve-time study (example script writing an SVG to `docs/figures/`, using synthetic data), documenting the reduction trade-off.
- [x] Token-gated **integration**: generate + reduce on real ENTSO-E BE/NL forecasts, assert the trade-off curve shape holds (skipped in CI, nothing committed). *(`tests/integration/test_scenarios_live.py`; verified live 2026-07-10.)*
- [x] `ruff`/`format`/`lint-imports` (scenarios contract KEPT)/`lint_docs` clean; all R1 and R2.1 gates unchanged.

## Golden oracles

Unlike R2.1's coverage, scenario *reduction* on a tiny fixed set has an exact answer, so R2.2 gets a real golden oracle, not only a statistical gate.

| # | inputs | expected | why this case |
|---|--------|----------|---------------|
| 1 | 3 one-period scenarios, values `[0, 1, 10]`, probs `[0.4, 0.4, 0.2]`, reduce to 2 via forward selection, `p = 1` | the deleted scenario and the exact redistributed probabilities + the resulting Kantorovich distance, all computed by hand | pins the forward-selection choice **and** the redistribution rule (deleted mass moves to its nearest kept path) against arithmetic, not a fit |
| 2 | any set reduced to `n_reduced == S` | the original set unchanged, distance `0.0` | reduction to full size is the identity |

(Exact numbers for oracle 1 are filled in during test-writing, computed by hand from the Kantorovich formula, then locked.)

## Property tests

- **Valid measure:** `probs >= 0` and `sum(probs) == 1` (within tolerance) for both the generated and the reduced set.
- **Mass conservation under reduction:** the reduced probabilities sum to 1; every deleted scenario's mass is redistributed to a kept scenario (no mass created or lost).
- **Monotone trade-off:** the Kantorovich distance to the original set is non-increasing as `n_reduced` grows, and is `0` at `n_reduced == S`.
- **Reduction beats random:** the forward-selected subset's distance is `<=` that of a random subset of equal size (the reducer is doing work, not just subsampling).
- **Support respected:** every generated path lies within the forecaster's calibrated interval envelope at the nominal level (generation does not invent prices outside the R2.1 support beyond the stated coverage slack).
- **Determinism:** fixed seed + fixed inputs yield a bit-stable `ScenarioSet` and reduction (regression-testable).

## Acceptance gate

*Blocks:* R2.3. Every box must pass.

- [x] Golden oracle 1 (hand-computed forward-selection reduction) and oracle 2 (identity) pass at tolerance `1e-9`.
- [x] All property invariants hold across Hypothesis-seeded generation draws.
- [x] The reduced set preserves the original within the **stated Kantorovich tolerance** at `n_reduced = 50` on the synthetic series, and beats random subsetting.
- [x] A committed count-vs-distance (and count-vs-solve-time) figure documents the trade-off.
- [x] Lint/format/layers/docs-lint clean; no R1 or R2.1 gate weakened.

## Out of scope

- **Stochastic / recourse optimization**: R2.3 consumes this scenario set; R2.2 only builds it.
- **Multistage scenario trees / nested conditional structure**: single-stage day-ahead fan only.
- **Moment-matching generation and copula methods**: noted alternatives, not built.
- **ARIMA/GARCH parametric generator on raw prices**: a second generator path; deferred unless open question 2 flips.
- **Distributional distance choice beyond Kantorovich-p** (e.g. Fortet-Mourier metrics): the published stability bound uses Kantorovich; others are noted-not-built.

## Resolved decisions (reviewed 2026-07-08)

Phase-local decisions only; roadmap/positioning stays in the Tier 0 log.

1. **Governing reference under the textbook-first policy.** The canonical *method* references (Dupačová/Gröwe-Kuska/Römisch 2003; Heitsch-Römisch 2003) are papers, not textbooks. **Resolved: name Dupačová et al. / Heitsch-Römisch (2003) governing** for the reduction theory (the method is paper-defined and has no textbook substitute of equal precision), with King & Wallace subordinate for scenario-generation framing; the deviation from textbook-first is noted explicitly in `references.md`, exactly as R2.1 does for the López de Prado evaluation discipline. Sections/editions marked for verification before relying.
2. **Generation method.** **Resolved: residual-path bootstrap** off the R2.1 forecaster (self-contained, preserves intra-day error correlation, token-free-testable); independent per-hour sampling rejected (destroys temporal structure), ARIMA/GARCH deferred ([ADR-0017](../decisions/0017-residual-path-bootstrap-generation.md)).
3. **Reduction algorithm.** **Resolved: fast forward selection** (Heitsch-Römisch) primary with probability redistribution, **k-means** the compared baseline ([ADR-0018](../decisions/0018-forward-selection-over-kmeans.md)); forward selection carries the distance guarantee, k-means is the pragmatic reference point (mirrors ADR-0014's CQR-vs-split).
4. **Does the coverage/quality gate need the R2.3 solve to be meaningful?** **Resolved: no for R2.2.** The gate is distance-preservation + trade-off shape + reduction-beats-random, all computable without a stochastic objective. The decision-relevant "reduced set preserves the *dispatch value*" check is deferred to R2.3, where the stochastic program exists; the split is stated so the gate is honest about what it proves now.
5. **New dependency for the reducer.** **Resolved: pure numpy** for forward selection and the Kantorovich distance (small, exact, runs in the main CI job); the `forecast` group's scikit-learn is reused only for the k-means baseline, lazily imported so the primary path and its gates need no group. No `scipy` added.
